# MECC Backend Requirements - 100% FREE Stack
# No paid API services required!

# Core ML/DL (Already in your MVP)
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Transformers & NLP (Already in your MVP)
transformers>=4.30.0
tokenizers>=0.13.0

# Audio processing (Already in your MVP)
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# Speech-to-Text (Already in your MVP - FREE!)
openai-whisper>=20230314

# API Framework (Already in your MVP)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Database (Already in your MVP)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Data handling (Already in your MVP)
numpy>=1.24.0
pandas>=2.0.0

# Utilities (Already in your MVP)
python-dotenv>=1.0.0
pydantic>=2.0.0

# === NEW: FREE Alternatives ===

# FREE Text-to-Speech
piper-tts>=1.2.0  # Primary TTS (fast, good quality)
# TTS>=0.22.0     # Alternative: Coqui TTS (better quality, slower)

# Video processing
ffmpeg-python>=0.2.0  # Python wrapper for ffmpeg

# HTTP client for Ollama
requests>=2.31.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# Development tools (optional)
black>=23.0.0
flake8>=6.0.0

# === EXTERNAL DEPENDENCIES (Install separately) ===
# These are not Python packages but system tools:
#
# 1. Ollama (for FREE LLM - Llama 3.2)
#    Windows: Download from https://ollama.com/download
#    Mac/Linux: curl -fsSL https://ollama.com/install.sh | sh
#    After install: ollama pull llama3.2:3b
#
# 2. ffmpeg (for video processing)
#    Windows: choco install ffmpeg
#    Mac: brew install ffmpeg
#    Linux: sudo apt install ffmpeg
#
# 3. PostgreSQL (for database)
#    Windows: Download from postgresql.org
#    Mac: brew install postgresql
#    Linux: sudo apt install postgresql

# === REMOVED (No longer needed - were paid services) ===
# anthropic  # Was used for Claude API
# openai     # Was used for OpenAI API (GPT, TTS)
# elevenlabs # Was used for ElevenLabs TTS

# === TOTAL COST: $0 FOREVER! ===
